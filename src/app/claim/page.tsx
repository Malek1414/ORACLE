'use client';

import { ClaimFilingFlow } from '@/components/mobile/ClaimFilingFlow';

export default function ClaimPage() {
  return (
    <main style={{ background: 'var(--bg)' }}>
      <ClaimFilingFlow />
    </main>
  );
}
